-- vim.api.nvim_set_keymap("n", "<leader>tas", ":ASToggle<CR>", { desc = "[T]oggle [A]uto[S]ave" })
return {
	-- "pocco81/auto-save.nvim", -- not using because the defaults cannot be overridden.
	-- see https://github.com/Pocco81/auto-save.nvim/pull/55
	-- config = function()
	-- 	require('auto-save').setup({
	-- 		enabled = false,
	-- 		debounce_delay = 5000,
	-- 		hor
	-- 	})
	-- end
}
